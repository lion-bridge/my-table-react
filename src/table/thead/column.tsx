import React from "react";
import { useComponent } from "../hooks/useComponents";

function Column() {
    const {ComponentTh,} = useComponent();
  return(
    <ComponentTh>
        
    </ComponentTh>
  )
}

export default Column;
